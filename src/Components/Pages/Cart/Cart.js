import React from 'react';

const Cart = (props) => {
    const serviceItem= props.service;
    const {price} = serviceItem
   

 

    return (
        
       <div>

       </div>
    );
};

export default Cart;